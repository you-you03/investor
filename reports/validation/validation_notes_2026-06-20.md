# Validation Notes — 2026-06-20

## 今日確認したこと

- 6/20時点の `score_snapshots.json` を更新し、満期済みの週次リターンを反映した。
- `validation_2026-06-20.md` では、スコアの予測力は1〜2週より4週後で強く出ている。
- 4週後ICは `Spearman rho=+0.29, p=0.0121` で、弱〜中程度ながら統計的に正の相関あり。
- スコア閾値 `>=7.0` は引き続き有効。4週後平均は `>=7.0: +18.8%`、`<7.0: +3.3%`。
- `fundamentals` は最も効いているファクター。3週後 `rho=+0.46`、4週後 `rho=+0.47`。
- `catalyst` は弱い。3週後 `rho=-0.25`、4週後 `rho=-0.05`。
- `technical` は主因ではなく、エントリータイミング補助として扱うのが妥当。

## 判断方法への反映状況

- 現状の判断ロジックは、大枠では今日の発見を既に反映できている。
- `research_prompts.py` は `Fundamentals 30% / Catalyst 15% / Technical 10%` になっており、ファンダ重視・カタリスト割引の方向。
- `decision_prompts.py` は `score < 7.0 PASS`、`7.0-7.4 は慎重`、`8.2+ は fundamentals 必須` になっている。
- `decision_prompts.py` には、1週後はノイズ、3週後alphaを主指標、4週後を補助指標にする方針も入っている。

## 過剰調整を避けるための方針

- 今日時点では、ウェイトを追加変更しない。
- `Sentiment 20%` はやや高めに見えるが、まだサンプル不足。即時変更はしない。
- `score 7.0-7.4` は4週後平均が悪くないため、完全排除ではなく「慎重扱い」を維持する。
- `catalyst` は弱いが、既に15%まで下げているため、さらに下げる判断は次回以降の検証待ち。
- 次回確認では、特に以下を見る:
  - fundamentals の優位が継続するか
  - sentiment が本当に20%に値するか
  - catalyst の負相関が一時的か継続的か
  - `7.0-7.4` バケットを WAIT 寄りにしすぎて機会損失していないか

## 次回チェック項目

- `scripts/fetch_returns.py` を実行して新しい満期リターンを埋める。
- `scripts/validate_scores.py` で最新 validation を作る。
- 4週後IC、factor別rho、`>=7.0` vs `<7.0` の差をこのメモと比較する。
- 少なくともあと2〜3回分の validation が溜まるまでは、スコアウェイトの大幅変更は避ける。
